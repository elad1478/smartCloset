type Item @model{
  id: ID!
  name: String!
  image: String
  outfits: [Outfit!] @connection
}

type Outfit @model{
  id: ID!
  items: [Item!] @connection
  rating: Int
}

type History @model{
  id: ID!
  outfit: Outfit! @connection
  eventName: String
  date: String!
}

